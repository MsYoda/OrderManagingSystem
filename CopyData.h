#pragma once
#include "Products.h"

Product* CopyData(Product* data); // Скопировать данные из data в новый указатель
void CopyData(Product* source, Product* destination); // Скопировать данные из sourse в destination 